// ===========================================
// LOGIN-DATEN HIER ÄNDERN
// ===========================================

// Passwort für den Login (einfach hier ändern)
export const AUTH_PASSWORD = 'CODA2025%';

// Optional: Benutzername aktivieren (true/false)
export const REQUIRE_USERNAME = false;

// Optional: Benutzername (nur wenn REQUIRE_USERNAME = true)
export const AUTH_USERNAME = 'admin';

// Wie lange soll der Login gültig sein? (in Tagen)
export const LOGIN_EXPIRY_DAYS = 7;
